<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <!-- Custom CSS -->
    <style>
        .container {
            max-width: 600px;
            margin: 50px auto;
        }
        .todo-list {
            list-style: none;
            padding: 0;
        }
        .todo-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid #ccc;
            padding: 10px 0;
        }
        .todo-item input[type="checkbox"] {
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">Todo List</h1>
        <form id="todo-form" action="/addTodo" method="post">
            <div class="mb-3">
                <label for="todo-input" class="form-label">New Todo</label>
                <input type="text" class="form-control" id="todo-input" name="task" required>
            </div>
            <button type="submit" class="btn btn-primary">Add Todo</button>
        </form>
        <ul class="todo-list mt-4">
            <!-- Todo items will be dynamically added here -->
        </ul>
    </div>

    <!-- Bootstrap JS and jQuery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Custom JavaScript -->
    <!-- <script>
        $(document).ready(function() {
            // Submit form to add new todo
            $('#todo-form').submit(function(event) {
                event.preventDefault();
                var todoText = $('#todo-input').val().trim();
                if (todoText) {
                    addTodoItem(todoText);
                    $('#todo-input').val('');
                }
            });

            // Function to add new todo item
            function addTodoItem(text) {
                var listItem = $('<li class="todo-item d-flex align-items-center"></li>');
                var checkbox = $('<input type="checkbox" class="form-check-input">');
                var todoText = $('<span class="todo-text flex-grow-1"></span>').text(text);
                var deleteBtn = $('<button class="btn btn-danger">Delete</button>');

                listItem.append(checkbox);
                listItem.append(todoText);
                listItem.append(deleteBtn);

                $('.todo-list').append(listItem);
            }

            // Delete todo item when delete button is clicked
            $(document).on('click', '.todo-item button', function() {
                $(this).closest('.todo-item').remove();
            });
        });
    </script> -->
</body>
</html>
